using System.Diagnostics;

namespace iPath.Application.Features.Users;

/*
[DebuggerDisplay("{GroupId} => {Role}")]
public record GroupMemberDto(Guid GroupId, Guid UserId, eMemberRole Role, string? Username = null);
*/