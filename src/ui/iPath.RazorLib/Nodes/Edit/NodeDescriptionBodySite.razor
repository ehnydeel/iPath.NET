@namespace iPath.Blazor.Componenents.Nodes

@if (Group is not null && Group.Settings.DescriptionWithBodySite)
{
    @if (Group.Settings.DescriptionGroupedBodySiteInput)
    {
        <TopoDetailLookup @bind-Code="Model.BodySite" />
    }
    else
    {
        <TopoLookup Label="Body Site" Clearable="true" Variant="Variant.Outlined" Dense="true" ShrinkLabel="true" @bind-Value="Model.BodySite" Margin="@Margin" />
    }
}

@code {
    [Parameter, EditorRequired]
    public GroupDto Group { get; set; }

    [Parameter, EditorRequired]
    public NodeDescription Model { get; set; }

    [Parameter]
    public Margin Margin { get; set; } = Margin.Dense;

    protected override void OnParametersSet()
    {
        var grp = Group?.Name;
        base.OnParametersSet();
    }
}
